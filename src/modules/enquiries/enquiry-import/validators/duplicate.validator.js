/**
 * Duplicate checks (in-file phone, then existing admission inquiries for the school)
 * are implemented in `../services/validation.service.js` so DB access stays in one place.
 */
module.exports = {};
